import { createTheme } from '@mui/material/styles';

export const appTheme = createTheme({
    palette: {
        primary: {
            main: '#f9c90d',
        },
        secondary: {
            main: 'rgba(113,127,253,0.84)',
        },
    },
});
